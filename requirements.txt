httpx>=0.27
fastapi>=0.139.0
uvicorn[standard]>=0.51.0
python-multipart>=0.0.32
pandas>=3.0.3
lightgbm>=4.3
scikit-learn>=1.4
numpy>=2.3.5
pyproj>=3.6

# 매출 분석 비동기 처리
celery>=5.6
redis>=8.0

# 생존분석 (Cox 시변 공변량 모형)
lifelines>=0.28.0
statsmodels>=0.14.0
scipy>=1.12.0

# OCR
paddlepaddle>=3.3.1
paddleocr>=3.7.0
protobuf>=7.35.1
huggingface_hub>=1.23.0
opencv-python>=5.0.0.93
matplotlib>=3.11.0
pillow>=12.3.0

openai>=1.0
langgraph>=1.0
langgraph-checkpoint-sqlite>=3.0
langsmith>=0.4

torch>=2.2

# RAG 검색(rag/retriever.py) — manifest.json 기준 BAAI/bge-m3 서빙에 필요
sentence-transformers>=3.0

# AI 상품 이미지 생성(app/product_image)에 필요.
# rembg 2.0.77부터 onnxruntime이 install_requires에서 빠져 extras로 분리됨 — 명시하지 않으면
# 백엔드 없이 설치되어 임포트 시점에 sys.exit(1)로 죽는다(CPU 전용, GPU는 이 프로젝트 범위 밖).
rembg[cpu]>=2.0
pillow>=10.0
requests>=2.31
python-dotenv>=1.0
