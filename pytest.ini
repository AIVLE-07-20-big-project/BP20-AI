[pytest]
pythonpath = .
# testpaths 를 지정하지 않으면 scripts/ops/s3_smoke_test.py 처럼 *_test.py 규칙에
# 걸리는 운영 점검 스크립트까지 수집되어 실제 S3 를 호출한다.
testpaths = tests
markers =
    integration: 실제 data/model 산출물에 의존하는 느린 종단간 테스트
